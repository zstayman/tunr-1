<%= form_for @artist do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :genre %>
  <%= f.text_field :genre %>

  <%= f.label :photo_url %>
  <%= f.url_field :photo_url %>

  <%= f.submit "Add Artist" %>
<% end %>