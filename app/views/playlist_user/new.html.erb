<%= form_for [@share] do |f| %>

  <%= f.label :users %>
  <%= f.select :users, options_from_collection_for_select(User.all, :id, :first_name, :last_name), {}, { multiple: true } %>

  <%= f.submit "Share" %>
<% end %>